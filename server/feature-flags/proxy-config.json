{
  "$schema": "https://docs.getunleash.io/schemas/proxy-config-schema.json",
  "description": "UnaMentis Unleash Proxy Configuration",

  "unleash": {
    "url": "http://unleash:4242/api",
    "apiToken": "*:*.unleash-insecure-admin-api-token",
    "instanceId": "unamentis-proxy"
  },

  "proxy": {
    "port": 3063,
    "clientKeys": ["proxy-client-key"],
    "enableAllEndpoint": true,
    "metricsInterval": 30000,
    "environment": "development"
  },

  "server": {
    "trustProxy": true,
    "enableRequestLogger": false
  },

  "cache": {
    "refreshInterval": 5000,
    "ttl": 60000
  },

  "comments": {
    "note": "This is a reference configuration. The actual proxy is configured via environment variables in docker-compose.yml",
    "production_tokens": "In production, replace insecure tokens with proper secrets",
    "client_keys": "iOS and Web clients use 'proxy-client-key' to authenticate with the proxy"
  }
}
